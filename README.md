# WordPressPlugIn

Repository containing the WooCommerce plugin `woo-delivery-scheduler`.

## Build installable zip

Run:

```bash
./scripts/build-plugin-zip.sh
```

This generates `woo-delivery-scheduler.zip` in the repository root for WordPress upload, without committing the binary to git.
